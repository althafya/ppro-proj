apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbchart.fullname" . }}
  labels:
    {{- include "dbchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dbchart.selectorLabels" . | nindent 4 }}
